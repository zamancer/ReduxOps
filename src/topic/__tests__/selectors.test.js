import * as uut from '../reducer'
import { Selector } from 'redux-testkit'

describe('Topic Selectors', () => {
    // Here you can define selector function tests
    // E.g.
    // it('should select some data', () => {
    //     const expectedData = {};
    //     const currentState = {};
    //     Selector(uut.someSelector).expect(currentState).toReturn(expectedData)
    // })
})
